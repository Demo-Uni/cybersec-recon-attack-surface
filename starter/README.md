# Map attack surfaces using Python recon tools

Complete the functions in `main.py` to make all tests pass.

## Setup
```bash
pip install -r requirements.txt
```

## Run Tests
```bash
pytest tests/ -v
```
